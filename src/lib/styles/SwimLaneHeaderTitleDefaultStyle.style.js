import Style from '../../../styles/Style'
import { Colors } from '../../../styles/Colors'

export const SwimLaneTitleDefaultStyle = {
    swimlaneTitleText: {
        color: Colors.TEXT_SECONDARY,
        marginLeft: Style.ratio(80),
        marginRight: Style.ratio(50)
    }
}
